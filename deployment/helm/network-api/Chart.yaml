apiVersion: v2
name: network-api
description: Trust-Network-Courts network-api — the JN Smart-Edge mTLS enforcer (RequireAndVerifyClientCert).
type: application

# Chart version. Bump on every chart change.
version: 0.1.1

# Tracks the network-api image tag this chart's defaults target.
# Override per-deploy via .Values.image.tag.
appVersion: "0.0.33"

kubeVersion: ">= 1.23.0"

home: https://github.com/baseproof/trust-network-courts
sources:
  - https://github.com/baseproof/trust-network-courts

maintainers:
  - name: baseproof

annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/category: security
