apiVersion: v2
name: aggregator
description: Trust-Network-Courts aggregator — a rebuildable read-projection ingester.
type: application

# Chart version. Bump on every chart change.
version: 0.1.1

# Tracks the aggregator image tag this chart's defaults target.
# Override per-deploy via .Values.image.tag.
appVersion: "0.0.33"

kubeVersion: ">= 1.23.0"

home: https://github.com/baseproof/trust-network-courts
sources:
  - https://github.com/baseproof/trust-network-courts

maintainers:
  - name: baseproof

annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/category: security
